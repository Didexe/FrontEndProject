class Slide {
    constructor(slideImage, slideTitle, slideText) {
        this.slideImage = slideImage;
        this.slideTitle = slideTitle;
        this.slideText = slideText;
    }
}

module.exports = {
    Slide,
}